# 팀 데몬스트레이션 진행 도구 — 기획서

> 사회자(교사)가 조건문을 던지고, 답과 점수를 부르고, 손 든 모둠에 점수를 얹는
> 한 판의 흐름을 화면 하나로 굴리는 도구.

---

## 1. 왜 만드는가

칠판 스코어보드의 문제는 셋이다.

- 사회자가 **분필을 잡는 순간 시선이 학생에게서 떠난다**
- 8칸을 도는 동안 **몇 번째 칸인지 아이들도 교사도 헷갈린다**
- 조건문을 즉석에서 떠올리느라 **템포가 끊긴다**

이 도구는 사회자가 **버튼만 누르면 진행되도록** 세 가지를 대신 들어준다:
조건문 제시 · 진행 위치 표시 · 점수 누적.

---

## 2. 사용 시나리오 (한 판)

| 단계 | 사회자 행동 | 화면 반응 |
|---|---|---|
| 준비 | `새 판 시작` → 모둠명 입력, 칸 수(4 / 6 / 8) 선택 | 모둠 카드 생성 |
| 조건 제시 | `다음 조건` 클릭 | 조건문이 큰 글씨로 뜸 (1/8) |
| (선택) 보너스 걸기 | 추가 점수만 먼저 선언 | `★ 숨은 보너스 300점` 표시 |
| 작성 대기 | (아이들이 백지에 기록) | 타이머 선택적 표시 |
| **채점 — 여러 번 반복** | 답 확정 → 손 든 모둠 카드 클릭 → `반영` | 점수 가산, 카드 반짝, 부른 답이 목록에 쌓임 |
| 조건 종료 | `다음 조건` 클릭 | 답 목록 비우고 다음 조건으로, 진행 바 채워짐 |
| 마무리 | `결과 보기` | 순위 정렬 + 라운드별 기록 |

### 조건 하나에 채점은 여러 번 일어난다

아이들은 백지에 답을 **여러 개** 쓴다. 사회자가 답을 하나씩 부르면 그 답을 가진 모둠이 손을 든다.
조건당 채점 5~10회가 보통이고, **이 반복이 이 도구가 실제로 도는 자리다.**

- 언제 다음 조건으로 넘어갈지는 **사회자가 정한다.** 자동 종료 없음
- 같은 답을 두 번 부르지 않도록 **이미 부른 답을 화면에 쌓아 둔다** (§3)

**핵심 조작 원칙: 채점 한 번은 클릭 4번 이내.**
정답 칩 1 + 모둠 카드 1~2 + `반영` 1. 모둠 다중 선택이 있는 한 3번은 지킬 수 없다.

---

## 3. 화면 구성 (단일 화면 · 투사 겸용)

```
┌────────────────────────────────────────────────┐
│  조건 3 / 8              ★ 숨은 보너스 300점    │  ← 진행 바 + 걸어 둔 보너스
│  ┌──────────────────────────────────────────┐  │
│  │   ㄱ으로 시작하는 동물                    │  │  ← 조건문 (초대형)
│  └──────────────────────────────────────────┘  │
│                                                │
│  (개) (곰) (고래) (거북) (기린) (까치)          │  ← 정답 칩 · 부른 답은 회색
│                                                │
│  답 [ 기린      ]   점수 [ 100 ] [ 반영 ]       │  ← 사회자 입력줄
│                                                │
│  나온 답 ▸ 곰 50 · 개 30 · 고래 50              │  ← 이 조건에서 처리한 답
│                                                │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐                  │
│  │무지개│ │구름 │ │번개 │ │별  │                │  ← 모둠 카드(클릭=선택)
│  │ 350 │ │ 200 │ │ 400 │ │250 │                │
│  └────┘ └────┘ └────┘ └────┘                  │
│                                                │
│  [‹이전] [ 다음 조건 ] [ 보너스 ] [ 되돌리기 ] [ 결과 ] │
└────────────────────────────────────────────────┘
```

- 모둠 카드는 **탭/클릭으로 토글 선택** (여러 모둠 동시 선택 가능)
- 점수는 **최근 사용값이 기본 유지** — 같은 점수 연속 부를 때 재입력 없음
- **정답 칩을 누르면 답·점수가 한 번에 입력**되고, 반영이 끝난 칩은 회색으로 죽는다
- **`나온 답` 줄**이 이 조건에서 이미 처리한 답을 보여 준다. 사회자가 중복해서 부르는 것을 막는 장치이자, 다음 조건으로 넘어갈 때를 판단하는 근거다
- 투사 시 뒷자리에서 읽히도록 조건문 최소 5rem, 모둠 점수 3rem
  (768px 태블릿에서는 과하므로 `clamp()`로 줄인다)

---

## 4. 기능 명세

### 4.1 필수 (v1)

| 기능 | 설명 |
|---|---|
| 모둠 설정 | 2~8모둠, 이름 직접 입력 (기본값 A/B/C…) |
| 칸 수 설정 | 4칸 / **6칸 기본** / 8칸 (시간 예산은 §4.5 참고) |
| 조건문·정답지 관리 | 프리셋 불러오기 + 교사 직접 입력(조건문 + 예상 답 목록) |
| 조건 뽑기·섞기 | 칸 수만큼 뽑되 **세트 배분표를 지킨다** (아래). 섞기는 세트 **안에서만** 돈다 |
| 정답 칩 채점 | 정답지의 답을 **칩 버튼**으로 띄워, 누르면 답·점수가 자동 입력됨 |
| 부른 답 표시 | 이 조건에서 이미 처리한 답은 **칩이 회색**으로 죽고 `나온 답` 줄에 쌓임 |
| 직접 채점 | 정답지에 없는 답이 나오면 답·점수 직접 입력 (그리고 그 자리에서 정답지에 추가) |
| 채점 | 답·점수 확정 → 모둠 다중 선택 → 반영 |
| **보너스 콜** | 점수를 먼저 걸어 두고 답은 나중에 공개해 채점 (§4.4) |
| **되돌리기** | 직전 **채점** 취소. 여러 번 누르면 계속 거슬러 올라간다 |
| **이전 조건** | 조건 이동은 되돌리기와 **별개 버튼.** 잘못 넘긴 것을 되돌리는 용도 |
| 진행 표시 | 현재 칸 / 전체 칸 |
| 결과 화면 | 순위 + 모둠별 라운드 획득 내역 |
| 상태 유지 | localStorage 자동 저장 (새로고침·꺼짐 대비). **저장 실패는 조용히 넘어간다** — 수업 중 alert 금지 |
| **새 판 시작** | 상태 전체 초기화. 1교시 3반 → 2교시 4반이 기본 상황이라 없으면 자동 저장이 오히려 방해가 된다 |

### 4.2 있으면 좋은 것 (v2)

- **자동 점수 제안**: 정답지에 없는 답일 때, 손 든 모둠 수로 역산 (1모둠 100 / 2모둠 50 / 3모둠 이상 30)
- **정답지 자동 학습**: 그 자리에서 매긴 답을 정답지에 저장해 다음 판부터 칩으로 뜸
- 작성 타이머 (30초/60초, 종료음)
- 조건 세트 JSON 내보내기/불러오기 (다른 선생님께 공유)
- 결과 화면 이미지 저장

### 4.3 넣지 말 것

- 로그인, 서버 DB — 수업 40분짜리 도구에 과함
- 학생 개별 단말 접속 — 백지에 손으로 쓰는 것이 이 놀이의 본질
- 애니메이션 과다 — 시선이 화면에 묶이면 놀이가 죽는다
- **감점** — 【경쟁】 스위치가 【포기】로 바뀐다. 오조작 교정은 되돌리기가 맡는다

### 4.3-1 세트 배분표

프리셋 10개는 몸풀기 4 (1·2·3·5) / 다문화 4 (6·7·8·9) / 몸 2 (4·10)로 나뉜다.
칸 수만큼 뽑을 때 이 비율을 지킨다.

| 칸 수 | 몸풀기 | 몸 | 다문화 | 배치 순서 |
|---|---|---|---|---|
| 4 | 2 | 1 | 1 | 몸풀기 · 몸풀기 · 몸 · 다문화 |
| 6 | 2 | 1 | 3 | 몸풀기 2 → 몸 1 → 다문화 3 |
| 8 | 3 | 2 | 3 | 몸풀기 3 → 몸 2 → 다문화 3 |

**순서 원칙 — 몸풀기 앞, 몸 가운데, 다문화 뒤.**

- **몸풀기가 앞** — 답이 넘쳐서 누구도 빈칸으로 남지 않는다. 첫 칸부터 빈칸이면 분위기가 죽는다
- **몸이 가운데** — 글자를 못 써도 그려서·흉내 내서 낼 수 있다. 언어의 문턱이 가장 낮아 완충 구간이 된다
- **다문화가 뒤** — 이 놀이의 목적지다. 분위기가 올라온 뒤에 와야 100점이 터진다

**어느 칸 수에서도 몸 세트가 최소 1개 들어간다.** 단순히 「몸풀기 앞, 다문화 뒤」로만
뽑으면 8칸에서 4+4로 채워져 몸 세트가 통째로 빠진다. 그러면 언어 문턱이 가장 낮은
세트가 사라지는 셈이라 §6의 의도와 정반대가 된다.

섞기는 **세트 안에서만** 돈다. 세트 간 순서는 고정이다.

### 4.4 보너스 콜 (재미 장치)

평소 채점은 **답 → 점수** 순서다. 보너스 콜은 그 순서를 뒤집는다.

1. 사회자가 `보너스 걸기`를 눌러 **점수만 먼저 선언한다** — 화면에 `★ 숨은 보너스 300점`
2. 답이 무엇인지는 밝히지 않는다
3. 사회자가 원하는 때에 그 답을 공개하고, 가진 모둠을 골라 `반영`

**왜 점수를 먼저 거는가**

- 값이 먼저 뜨면 아이들이 술렁인다. 답을 모르니 **드문 답을 쓰는 쪽으로 움직인다**
- 이건 §6 다문화 세트의 목적과 같은 방향이다. 「아는 아이만 웃는」 상황이 아니라 「안 나올 것 같은 답을 적어 보는」 상황을 만든다
- 아이들이 쓰기 **전에** 걸어도 되고, 다 쓴 **뒤에** 걸어도 된다. 전자는 유도 장치, 후자는 긴장 장치다. 둘 다 같은 버튼으로 된다

**규칙**

- 한 조건에 보너스는 **하나만.** 여러 개면 값이 흔해져서 장치가 죽는다
- 보너스 점수는 정답지 최고점(100)보다 확실히 높게 — 200 / 300 권장
- 아무도 못 맞히면 **그냥 넘어간다.** 「아무도 없네」 한마디가 다음 조건의 긴장이 된다
- 보너스도 §9 규칙을 따른다. 여러 모둠이 맞히면 **전원이 그 점수를 그대로** 받는다

### 4.5 시간 예산

§4.3이 「수업 40분짜리 도구」를 근거로 기능을 쳐냈으니, 한 판이 몇 분인지도 적어 둔다.

```
조건 1개 = 작성 30~60초 + 답 5~8개 호출 × 15초 ≒ 2~3분

4칸 ≒ 10~14분   |   6칸 ≒ 15~21분   |   8칸 ≒ 20~28분
```

여기에 설명·정리·마무리가 붙는다.

- **4칸** — 다른 활동에 곁들일 때. 도입이나 정리 활동
- **6칸** — 기본값. 40분 한 차시에 설명·마무리까지 넣어 딱 맞는다
- **8칸** — 이 놀이만으로 한 차시를 꽉 채울 때. 시간이 빠듯하니 답 호출을 짧게 끊는다

---

## 5. 데이터 구조

```js
// 게임 상태 (localStorage 키: 'team-demo-state')
{
  teams: [
    { id: 't1', name: '무지개' },       // 점수는 저장하지 않는다 — history에서 계산
    { id: 't2', name: '구름'   }
  ],
  cells: 6,              // 칸 수 4 | 6 | 8 (기본 6)
  currentIndex: 2,       // 0-based 현재 조건 위치
  bonus: {               // 걸어 둔 숨은 보너스, 없으면 null
    points: 300,
    promptIndex: 2       // 어느 조건에 걸었는지
  },
  prompts: [             // 조건문 + 정답지
    {
      id: 'p1',                         // 정답지 자동 학습(v2)이 참조할 고정 키
      set: '몸풀기',                     // 몸풀기 | 다문화 | 몸 — 배치 순서에 쓰임
      text: 'ㄱ으로 시작하는 동물',
      answers: [                        // 흔한 답은 낮게, 드문 답은 높게
        { word: '개',   points: 30 },
        { word: '곰',   points: 50 },
        { word: '기린', points: 100 }
      ]
    }
  ],
  history: [             // 되돌리기 · 결과 화면용 · 점수의 유일한 출처
    // teamIds에 든 모둠은 points를 각각 그대로 받는다 (나눠 갖지 않음)
    { index: 0, promptId: 'p1', prompt: 'ㄱ으로 시작하는 동물',
      answer: '곰', points: 50, teamIds: ['t1','t3'], bonus: false }
  ]
}
```

**`history`가 점수의 유일한 출처다.** `teams[].score`를 따로 두면 둘이 어긋나는
버그가 반드시 나므로 저장하지 않는다. 감점이 없어 `points`는 항상 양수이고,
점수 계산은 `history`를 훑어 `teamIds`에 자기가 든 항목의 `points`를 더하는 것이 전부다.

```js
const scoreOf = (teamId) =>
  history.filter(h => h.teamIds.includes(teamId))
         .reduce((sum, h) => sum + h.points, 0)
```

그래서 **되돌리기는 `history.pop()` 한 줄이고, 몇 번이든 거슬러 올라갈 수 있다.**
조건 이동(`currentIndex`)은 `history`와 무관하므로 되돌리기와 별개 버튼으로 둔다.

---

## 6. 기본 조건문 10개 (프리셋 · 정답지 포함)

점수는 **30 / 50 / 100** 세 단계. 흔한 답은 30, 웬만한 답은 50, 잘 안 나올 답은 100.
정답지는 정답 판정용이 아니라 **사회자의 점수 힌트**다. 없는 답이 나오면 그 자리에서 매기면 된다.

| # | 조건문 | 30점 (흔한 답) | 50점 | 100점 (드문 답) |
|---|---|---|---|---|
| 1 | ㄱ으로 시작하는 동물 | 개, 고양이, 곰 | 고래, 거북, 개구리 | 기린, 까치, 게, 가재 |
| 2 | ㅁ으로 시작하는 음식 | 만두, 미역국 | 물냉면, 마라탕, 무 | 멸치, 마늘, 매생이, 묵 |
| 3 | 교실에서 볼 수 있는 것 | 책상, 의자, 칠판 | 사물함, 시계, 컴퓨터 | 소화기, 화분, 우산꽂이, 콘센트 |
| 4 | 노란색인 것 | 바나나, 병아리 | 개나리, 레몬, 참외 | 은행잎, 택시, 안전모, 유치원 버스 |
| 5 | 바다에 사는 동물 | 상어, 문어, 고래 | 오징어, 게, 새우 | 해파리, 불가사리, 말미잘, 성게 |
| 6 | 두 글자 나라 이름 | 미국, 일본, 중국 | 태국, 인도, 영국 | 호주, 독일, 몽골, 케냐 |
| 7 | 인사말을 아는 나라 | 미국(헬로), 일본(곤니치와) | 중국(니하오), 베트남(신짜오) | 태국(사와디), 몽골(샌배노), 필리핀(쿠무스타) |
| 8 | 먹어 본 다른 나라 음식 | 피자, 스파게티 | 쌀국수, 초밥, 카레 | 타코, 케밥, 반미, 팟타이 |
| 9 | 국기에 빨간색이 있는 나라 | 대한민국, 일본 | 중국, 미국, 베트남 | 캐나다, 태국, 튀르키예, 싱가포르 |
| 10 | 몸으로 낼 수 있는 소리 | 박수, 발 구르기 | 휘파람, 무릎 치기 | 손가락 튕기기, 혀 차기, 볼 두드리기 |

**세 묶음으로 나눠 담는다**

- **몸풀기 세트** (1·2·3·5) — 정답이 넘쳐서 누구도 빈칸으로 남지 않는다. 첫 두세 칸에 배치.
- **다문화 세트** (6·7·8·9) — 답이 **한국에서 나고 자란 아이에게 유리하지 않다.** 이주배경 학생이 오히려 100점짜리를 아는 순간이 이 놀이의 목적지다.
- **몸 세트** (4·10) — 글자를 못 써도 그려서, 흉내 내서 낼 수 있다. 언어의 문턱이 가장 낮다.

### 교사 직접 입력

조건문 하나당 이렇게 넣는다.

```
조건문: 우리가 배운 통일이 되면 좋은 점
답 30점: 이산가족이 만난다, 전쟁이 없어진다
답 50점: 기차로 유럽에 간다
답 100점: 군인이 줄어 그 돈을 다른 데 쓴다
```

- 답 없이 조건문만 넣어도 진행된다 (점수는 그때그때 사회자가)
- 조건 세트는 JSON으로 내보내 다른 선생님께 넘길 수 있다
- **주의: 답이 하나뿐인 조건문은 넣지 않는다.** 아는 아이만 웃는다.

---

## 7. 기술 스택

- **단일 HTML 파일** (HTML + CSS + Vanilla JS) — GitHub Pages 바로 배포
- 프레임워크 없음, 빌드 없음
- 반응형: 교실 TV(1920px) 우선, 태블릿(768px) 대응
- 오프라인 동작 (외부 요청 0건)

---

## 8. 개발 단계

1. **1단계** — 모둠 설정 + 새 판 시작 + 점수 가산 + 되돌리기
   (`history` 하나로 도는 스코어보드. 이것만으로 이미 쓸 만하다)
2. **2단계** — 조건문 흐름 + 진행 바 + 정답 칩 + **나온 답 표시**
   (조건당 채점 N회 루프가 여기서 완성된다 — §2)
3. **3단계** — 프리셋·세트 배치, **보너스 콜**, 결과 화면 다듬기
4. **4단계 (v2)** — 자동 점수 제안, 정답지 자동 학습, 타이머, JSON 내보내기

1단계만 나와도 다른 놀이에 그대로 재사용 가능하다.
**보너스 콜은 3단계다** — 진행이 안정된 다음에 얹어야 재미 장치로 작동한다.

---

## 9. 결정 사항

- [x] **여러 모둠이 같은 답 → 전원에게 그 점수를 그대로 준다.** 나눠 갖지 않는다.
      희소성은 정답지의 30 / 50 / 100이 이미 담당하므로 여기서 또 깎지 않는다.
      손 든 모둠이 셋이고 답이 50점이면, 세 모둠 모두 50점씩 받는다.
      보너스 콜(§4.4)도 같은 규칙을 따른다.
- [x] **감점 없음.** 오조작 교정은 감점이 아니라 되돌리기가 맡는다.
- [x] **꼴찌는 모둠 수에 따라 다르게 다룬다.**
      3모둠 이하면 등수 강조 없이 전원 나열, 4모둠 이상이면 상위만 강조하고
      나머지는 등수 없이 **획득 내역만** 보여 준다.
      「상위 3모둠만 강조」로 못 박으면 3모둠일 때 전원이 상위라 규칙이 무의미해진다.

결과 화면의 초점은 등수가 아니라 **「우리 모둠이 어느 조건에서 무엇으로 점수를 얻었나」**다.
특히 다문화 세트에서 100점이 어느 모둠에서 나왔는지가 보이면, 그 자리가 이 놀이의 목적지다.
