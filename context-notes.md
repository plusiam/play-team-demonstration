# 맥락 노트 — 팀 데몬스트레이션 진행 도구

작업 중 내린 결정과 **그 이유**를 남긴다. 다음 세션이 결정을 다시 도출하지 않도록.
새 결정은 아래에 계속 덧붙인다.

---

## 2026-07-31 — 기획서 검토와 초기 결정

### 결정 1. 여러 모둠이 같은 답 → 전원에게 그 점수를 그대로

나눠 갖지 않는다. 손 든 모둠이 셋이고 답이 50점이면 세 모둠 모두 50점.

**왜**
- 나눗셈 나머지 처리와 암산이 사회자 템포를 죽인다. 이 도구의 목적이 템포 유지다
- 희소성은 이미 정답지의 30 / 50 / 100이 담당한다. 여기서 또 깎으면 이중 적용이다
- 데이터 구조가 이미 이걸 전제하고 있었다 (`{ points, teamIds }`). 결정을 스키마에 맞춘 게 아니라, 스키마가 옳았음을 확인한 것

**파급** — `history` 항목 하나에 점수는 하나. 모둠별 개별 점수 필드가 필요 없다.

### 결정 2. 감점 없음

**왜**
- 감점이 있으면 【경쟁】 스위치가 【포기】로 바뀐다. 뒤처진 모둠이 시도를 멈춘다
- 오조작 교정은 감점이 아니라 되돌리기의 몫이다. 역할이 겹칠 이유가 없다

**파급** — `points`가 항상 양수다. 점수 계산이 단순 덧셈으로 끝난다.

### 결정 3. 꼴찌는 모둠 수에 따라 다르게

3모둠 이하면 등수 강조 없이 전원 나열. 4모둠 이상이면 상위만 강조하고
나머지는 등수 없이 획득 내역만.

**왜** — 원안의 「상위 3모둠만 강조」는 3모둠일 때 전원이 상위라 규칙이 무의미해진다.

### 결정 4. `teams[].score`를 저장하지 않는다

`history`가 점수의 유일한 출처다.

**왜**
- `score`를 따로 저장하면 `history`와 어긋나는 버그가 반드시 난다. 진실의 출처가 둘이면 언젠가 갈라진다
- 파생값으로 두면 되돌리기가 `history.pop()` 한 줄이 된다. **되돌리기 횟수 제한이 사라지는 것도 여기서 공짜로 나온다**

원래 기획서는 「직전 1회만 취소」였는데, 이 구조에서는 오히려 제한하는 쪽이 코드가 더 든다.

### 결정 5. 되돌리기와 조건 이동은 별개 버튼

**왜** — 두 행동이 다르다. 「점수를 잘못 줬다」와 「조건을 잘못 넘겼다」는 각각 고쳐야 한다.
`currentIndex`는 `history`와 무관하므로 기술적으로도 분리가 자연스럽다.

### 결정 6. 조건 하나에 채점은 여러 번 (기획서의 가장 큰 구멍이었음)

원래 시나리오 표는 조건당 채점 1회처럼 그려져 있었다. 실제 놀이는 다르다.
아이들이 백지에 답을 여러 개 쓰고, 사회자가 하나씩 부르고, 가진 모둠이 손을 든다.
**조건당 채점 5~10회.**

**파급 (이게 화면 설계를 바꿨다)**
- 이미 부른 답을 화면에 쌓아야 한다 → `나온 답` 줄 신설, 정답 칩 회색 처리
- 조건 종료는 사회자 판단. 자동 종료를 넣으면 오히려 방해가 된다
- 「채점 한 번 = 클릭 3번」 원칙이 성립하지 않는다 → **4번으로 고침**
  (칩 1 + 모둠 카드 1~2 + 반영 1. 모둠 다중 선택이 있는 한 3번은 불가능)

### 결정 7. 보너스 콜 — 점수를 **먼저** 걸고 답은 나중에

**왜**
- 값이 먼저 뜨면 아이들이 술렁인다. 답을 모르니 **드문 답을 쓰는 쪽으로 움직인다**
- 이 방향이 §6 다문화 세트의 목적과 정확히 같다. 「아는 아이만 웃는」 게 아니라
  「안 나올 것 같은 답을 적어 보는」 상황을 만든다
- 쓰기 전에 걸면 유도 장치, 쓴 뒤에 걸면 긴장 장치. **같은 버튼으로 둘 다 된다**

**규칙과 이유**
- 한 조건에 하나만 — 여러 개면 값이 흔해져서 장치가 죽는다
- 정답지 최고점(100)보다 확실히 높게, 200~300
- 아무도 못 맞히면 그냥 넘어간다. 「아무도 없네」가 다음 조건의 긴장이 된다

### 결정 8. 칸 수 4 / 6 / 8, 기본 6

시간 예산에서 나온 값이다. 조건 1개 ≒ 2~3분.

| 칸 수 | 소요 | 쓰임 |
|---|---|---|
| 4 | 10~14분 | 다른 활동에 곁들일 때 |
| **6** | 15~21분 | **기본.** 40분 한 차시에 설명·마무리까지 딱 맞음 |
| 8 | 20~28분 | 이 놀이만으로 한 차시를 채울 때 |

### 결정 9. `새 판 시작` 버튼은 v1 필수

**왜** — 1교시 3반 → 2교시 4반이 기본 상황이다.
초기화 없는 localStorage 자동 저장은 도움이 아니라 **방해**가 된다.
4반 수업을 3반 점수 위에서 시작하게 된다.

같이 정한 것 — 학교 공용 PC·TV는 시크릿 모드거나 저장이 막혀 있을 수 있다.
**저장 실패는 조용히 넘어간다.** 수업 중 alert가 뜨면 그게 사고다.

### 결정 10. 세트 배분표 — 몸풀기 앞 · 몸 가운데 · 다문화 뒤

| 칸 수 | 몸풀기 | 몸 | 다문화 |
|---|---|---|---|
| 4 | 2 | 1 | 1 |
| 6 | 2 | 1 | 3 |
| 8 | 3 | 2 | 3 |

**왜 비율을 못 박았나** — 처음엔 「몸풀기 앞, 다문화 뒤」로만 적었다.
그런데 세트 크기가 4 / 4 / 2라서 8칸을 그 규칙으로 채우면 4+4가 되어
**몸 세트(4·10)가 통째로 빠진다.** 몸 세트는 §6이 「언어의 문턱이 가장 낮다」고 한 세트다.
글자를 못 써도 그려서·흉내 내서 낼 수 있는 칸이 사라지면 §6의 의도와 정반대가 된다.
그래서 **어느 칸 수에서도 몸 세트 최소 1개**를 조건으로 걸고 비율을 역산했다.

**왜 이 순서인가**
- 몸풀기가 앞 — 답이 넘쳐서 첫 칸부터 빈칸이 생기지 않는다
- 몸이 가운데 — 언어 문턱이 낮아 완충 구간이 된다
- 다문화가 뒤 — 목적지다. 분위기가 올라온 뒤에 와야 100점이 터진다

섞기는 세트 **안에서만** 돈다. 세트 간 순서는 고정.

### 결정 11. 화면 상태는 전부 `history`에서 파생한다

점수뿐 아니라 **`나온 답` 줄과 정답 칩의 회색 여부도** `history`에서 계산한다.
별도 `usedAnswers[]` 배열이나 `chip.used` 플래그를 두지 않는다.

```js
const usedThisPrompt = () => history.filter(h => h.index === state.currentIndex)
```

**왜** — 플래그를 따로 두면 되돌리기가 점수만 복구하고 **칩은 죽은 채로 남는다.**
결정 4에서 없앤 「진실의 출처가 둘」 문제가 다른 얼굴로 돌아오는 것이다.
게다가 이 버그는 조용해서 수업 중에야 발견된다.

### 결정 12. 보너스는 공개 이후 되돌리기로 복구하지 않는다

보너스 채점을 되돌리면 `history`에서 그 항목은 빠지지만 `state.bonus`는 살아나지 않는다.
다시 걸면 된다.

**왜** — 보너스 복구까지 `history`에 실으려면 「걸기」 자체를 이벤트로 기록해야 하는데,
그러면 점수와 무관한 항목이 `history`에 섞여 결정 4의 단순함이 깨진다.
버튼 한 번 다시 누르는 비용이 훨씬 싸다.

---

---

## 2026-07-31 — 구현 중 내린 결정

### 결정 13. 보너스 점수는 대화상자가 아니라 인라인 줄로 받는다

처음엔 `prompt()`로 받았다. 바꿨다.

**왜** — 이건 교실 TV에 **투사되는** 화면이다. 대화상자가 뜨면 조건문과 점수판을
통째로 덮는다. 아이들이 보고 있는 화면이 갑자기 회색 상자로 바뀌는 셈이다.
게다가 브라우저·키오스크 설정에 따라 대화상자가 차단되기도 한다.

`숨은 보너스 점수 [200] [300] [직접입력] [걸기] [닫기]` 한 줄로 바꿨다.
200·300이 버튼이라 클릭 두 번이면 걸린다.

**남겨 둔 것** — `새 판 시작`의 `confirm()`은 그대로 뒀다. 점수를 전부 지우는
되돌릴 수 없는 동작이라 한 번 막아 주는 값이 대화상자의 불편보다 크고,
한 차시에 한 번 있는 일이다.

### 결정 14. 진행 화면은 세로를 끝까지 채운다

`.wrap`을 `min-height:100vh` 플렉스로 두고 조건문 상자가 남는 세로를 흡수하게 했다.
글자 크기도 `min(6.4vw, 13vh)`로 가로·세로를 함께 본다.

**왜** — 처음엔 1920×1080에서 아래 35%가 비었다. 투사 도구에서 화면이 남는다는 건
**그만큼 글씨를 키울 수 있었는데 안 키웠다**는 뜻이다. 뒷자리 가독성이 이 도구의 요구사항이다.
지금은 1920에서 조건문 120px, 모둠 점수 57.6px가 나온다.

### 결정 15. 조건문 하나당 정답 칩은 정답지 전부를 한 줄에 편다

30 / 50 / 100을 구분해 접어 두지 않고 전부 노출한다.

**왜** — 사회자는 아이들이 부른 답을 **찾아서** 눌러야 한다. 접혀 있으면 펴는 동작이
클릭 수에 더해진다. 칩에 점수를 같이 적어 두면 굳이 나눠 놓을 이유가 없다.

### 결정 16. §6-7 인사말은 `{ word, hint }`로 나눴다

`미국(헬로)` 대신 `word:'미국', hint:'헬로'`. 칩에는 나라 이름이 크게, 인사말이 작게 붙는다.

**왜** — 괄호째 한 덩어리면 칩이 길어져 줄이 넘친다. 답으로 기록되는 것은 나라 이름이므로
`history`에도 깔끔하게 남는다.

---

## 보류 · 미결

- **v2 「자동 점수 제안」** (손 든 모둠 수로 역산) — 결정 1과 충돌하지 않음을 확인했다.
  모둠 수로 **값 하나를 정하고**, 그 값을 전원에게 똑같이 준다. v2에서 그대로 진행 가능
- **`prompts[].id`** — 진행 중에는 배열 인덱스로 충분하다.
  v2 「정답지 자동 학습」이 프리셋 편집 뒤에도 답을 되찾으려면 필요해서 미리 넣어 뒀다
- **§6-9 국기 문항 점수 배분** — 캐나다가 100점(드문 답)인데 초등학생에게는 잘 알려져 있다.
  미국이 50점인데 가장 흔하다. 다만 §6이 「정답지는 판정용이 아니라 사회자 힌트」라고
  못 박아 뒀으므로 실사용에는 지장 없음. 나중에 수업해 보고 조정
- ~~**§6-7 「인사말을 아는 나라」** 표기~~ → 결정 16에서 `{ word, hint }`로 정리됨
- **git 저장소가 아직 없다.** `git init`과 GitHub Pages 배포는 사용자 확인 후 진행
