# 개발 체크리스트 — 팀 데몬스트레이션 진행 도구

기획서 §8 개발 단계를 실행 단위로 쪼갠 목록.
각 항목은 **검증 방법**까지 적는다. 검증이 안 되면 체크하지 않는다.

> 진행 상황 — 0~3단계 완료 (2026-07-31). 남은 것은 README와 배포.

---

## 0단계 — 뼈대

- [x] `index.html` 단일 파일 생성 (HTML + CSS + JS 한 파일, 빌드 없음)
      → 브라우저로 열어 콘솔 에러 0건 확인
- [x] 상태 객체 `state` 정의 (기획서 §5 스키마 그대로)
- [x] `save()` / `load()` — localStorage 키 `team-demo-state`
      → 저장→복원 완전 일치 확인 (`currentIndex` · `bonus` · `history` 전부)
- [x] 저장 실패 시 조용히 넘어가기 (`try/catch`, alert 금지)
      → `setItem`이 예외를 던지도록 막아 두고도 앱이 계속 동작함을 확인

---

## 1단계 — 스코어보드

- [x] `새 판 시작` — 상태 전체 초기화
      → state 비움 · 저장소 비움 · 설정 화면 복귀 확인
- [x] 모둠 설정 화면 — 2~8모둠, 이름 입력 (기본값 A/B/C…)
- [x] 칸 수 선택 4 / 6 / 8 (기본 6)
- [x] 모둠 카드 토글 선택 (다중 선택)
- [x] 채점 반영 — 답·점수 확정 → 선택 모둠에 가산
      → 두 모둠 선택 + 50점 → **둘 다 50점씩** (기획서 §9) 확인
- [x] 점수는 `history`에서 계산 (`teams[].score` 저장 안 함)
      → `state.teams[0]`에 `score` 키 없음 확인
- [x] 되돌리기 — `history.pop()`, 횟수 제한 없음
- [x] 반영 시 카드 반짝

---

## 2단계 — 조건문 흐름

- [x] 조건문 초대형 표시 + 진행 바 (`조건 3 / 6`)
      → 1920×1080에서 조건문 120px · 모둠 점수 57.6px, 가로 넘침 없음
- [x] `다음 조건` / `‹이전` — 조건 이동
      → 이동해도 점수 그대로 (조건 이동 ≠ 되돌리기)
- [x] 정답 칩 — 누르면 답·점수 자동 입력
- [x] **부른 답 회색 처리** + `나온 답` 줄에 누적
      → 조건 넘기면 목록 비워짐 확인
- [x] 칩 상태와 `나온 답` 줄도 **`history`에서 파생** (별도 플래그 금지)
      → 반영(회색 칩 3) → 되돌리기 → **회색 칩 2로 되살아나고** 나온 답 줄에서도 사라짐
- [x] 직접 채점 — 정답지에 없는 답 입력 → 그 자리에서 정답지에 추가
- [x] 점수 최근값 유지

**2단계 완료 기준 — 통과**
- 6칸 · 4모둠 완주 + 되돌리기 2회 → 기대 총점과 화면 총점 일치
- 8칸 · 5모둠 완주 + 되돌리기 3회 → 일치 (history 21건)

---

## 3단계 — 프리셋 · 보너스 · 결과

- [x] 프리셋 10개 데이터 입력 (기획서 §6 표 그대로, `set` 태그 포함)
- [x] 칸 수만큼 뽑기 + **세트 배분표 준수** (기획서 §4.3-1)
      → 4 / 6 / 8칸 각각 200회 뽑아 ①첫 칸 항상 몸풀기 ②**몸 세트 항상 1개 이상**
      ③다문화 항상 뒤쪽 ④중복 없음 — 모두 통과
- [x] 진행 중 답 추가 — 정답지에 없는 답을 치고 반영하면 정답지에 들어가 칩으로 뜸
      → 그 **판 안에서만** 유지된다. 새 판을 시작하면 프리셋 원본으로 돌아간다
- [x] **교사 직접 입력 — 조건문 + 세트 + 30/50/100 답 목록** (기획서 §4.1)
      → 만들기·고치기·삭제 확인. 내 조건문은 `team-demo-custom`에 따로 저장되어
      `새 판`을 눌러도 남는다 (4개 만든 뒤 새 판 → 4개 그대로)
- [x] 조건문 출처 선택 — `기본 조건문` / `내 조건문` / `섞어서`
      → 내 조건문이 없으면 뒤 둘은 잠김. 꾸러미가 칸 수보다 적으면 시작 버튼 잠김
- [x] 출처를 바꿔도 세트 배분표 유지
      → 섞어서 6칸 200회: 배분표 2/1/3 정확, 세트 순서 유지, 내 조건 포함 123회
      → 내 조건문만 4칸 100회: 길이·중복없음·세트순서 모두 통과 (몸풀기가 1개뿐이라 보완 규칙 작동)
- [x] 편집 중 다시 그려도 타이핑이 살아남음
      → 조건문 입력 중 모둠 수 변경 → 입력 유지. 세트 변경 → 입력 유지
- [x] **보너스 걸기** — 점수만 먼저 선언, 화면에 `★ 숨은 보너스 N점`
      → 대화상자 대신 **인라인 줄**(200 / 300 / 직접)로 구현. 투사 화면을 덮지 않는다
- [x] 보너스는 한 조건에 하나만 (걸린 상태에서 버튼은 「취소」로 바뀜)
- [x] 보너스 공개 반영 → 여러 모둠이면 **전원 그 점수** · `history.bonus = true` · 배너 내려감
- [x] 결과 화면 — 순위 + 모둠별 라운드 획득 내역
      → 3모둠: 강조 0 · 등수 표기 없음 / 4·5모둠: 상위 3만 강조 — 확인

---

## 마무리

- [x] 콘솔 에러 0건
- [x] 375px / 768px / 1280px / 1920px 뷰포트 확인 — 깨짐·가로 스크롤 없음
- [x] 상대 경로만 사용 (외부 파일 참조 자체가 없음)
- [x] 외부 요청 0건 (단일 파일 · CDN 없음 · 폰트도 시스템 폰트)
- [x] README 작성 (목적 / 학년·교과 / 사용 시나리오 / 체험 링크 / 라이선스)
- [x] git 저장소 초기화 + GitHub Pages 배포
      → https://plusiam.github.io/play-team-demonstration/ · HTTP 200 · 콘솔 에러 0건
      → 배포본에서 시작·채점·되돌리기·보너스 동작 확인
      → 네트워크 요청은 문서 1건뿐 (폰트·CDN·분석 스크립트 없음)

---

## 검증 기록 (2026-07-31)

브라우저에서 실제 DOM을 클릭해 돌린 결과.

| 검사 | 결과 |
|---|---|
| 6칸·4모둠 완주 + 되돌리기 2회 총점 대조 | PASS |
| 8칸·5모둠 완주 + 되돌리기 3회 총점 대조 | PASS |
| 되돌리기 후 칩·나온 답 줄 복원 | PASS |
| 세트 배분표 (4/6/8칸 × 200회) | PASS |
| 보너스 걸기·공개·취소·직접입력·0점 거부 | PASS |
| 저장 → 복원 완전 일치 | PASS |
| 저장 차단 환경에서 앱 생존 | PASS |
| 새 판 시작 초기화 | PASS |
| 결과 화면 모둠 수별 강조 규칙 | PASS |
| 콘솔 에러 | 0건 |

**확인하지 못한 것** — 미리보기 창이 `navigate`로 페이지를 실제 재실행하지 않아,
「진짜 새로고침 후 이어서 하기」는 초기 진입 함수를 직접 호출해 대신 확인했다.
실제 브라우저에서 F5로 한 번 더 봐 주시면 좋다.
